[build]
target = "index.html"
dist = "dist"
public_url = "/"

[serve]
addresses = ["127.0.0.1"]
port = 3000

[watch]
watch = ["styles"]

[[hooks]]
stage = "pre_build"
command = "npm"
command_arguments = ["run", "build:css"]
